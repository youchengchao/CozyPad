{
  "name": "@cozypad/mobile",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "sync": "pnpm --filter @cozypad/app build && cap sync android",
    "apk": "pnpm run sync && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "@cozypad/app": "workspace:*"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2"
  }
}
